import * as React from "react";
import { StyleSheet, View } from "react-native";

const RicriminalFill = () => {
  return <View style={styles.ricriminalFill} />;
};

const styles = StyleSheet.create({
  ricriminalFill: {
    flex: 1,
    width: "100%",
    height: 24,
    overflow: "hidden",
  },
});

export default RicriminalFill;
